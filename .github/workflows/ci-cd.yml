name: CI CD

on:  
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [ created ]

env:
  DOTNET_VERSION: 5.0.300
  CONFIGURATION: Release

jobs:
  build-test:    
    runs-on: ubuntu-latest   
    name: Build and test
    steps:
    - name: Checkout code
      uses: actions/checkout@v2 
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }} 
        
    - name: Build
      run: dotnet build Arc.HttpHealthCheckDashboard.DI/Arc.HttpHealthCheckDashboard.DI.csproj --configuration ${{ env.CONFIGURATION }}

    - name: Test
      run: dotnet test Arc.HttpHealthCheckDashboard.DITests/Arc.HttpHealthCheckDashboard.DITests.csproj --configuration ${{ env.CONFIGURATION }}

  github-nuget-push:
    if: ${{ github.event_name == 'push' || github.event_name == 'release' }}
    runs-on: ubuntu-latest
    needs: build-test
    name: GitHub nuget push
    steps:
    - name: Checkout code
      uses: actions/checkout@v2 
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}    
        
    - name: Build
      run: dotnet build Arc.HttpHealthCheckDashboard.DI/Arc.HttpHealthCheckDashboard.DI.csproj --configuration ${{ env.CONFIGURATION }}

    - name: Create nuget package
      run: dotnet pack Arc.HttpHealthCheckDashboard.DI/Arc.HttpHealthCheckDashboard.DI.csproj --configuration ${{ env.CONFIGURATION }}

    - name: Push nuget package
      run: dotnet nuget push "Arc.HttpHealthCheckDashboard.DI/bin/Release/Arc.HttpHealthCheckDashboard.DI.1.0.0.nupkg" --api-key ${{ secrets.GITHUB_TOKEN }} --source "https://nuget.pkg.github.com/Arnab-Developer/index.json" --skip-duplicate

  nuget-org-push:
    if: ${{ github.event_name == 'release' }}
    runs-on: ubuntu-latest
    needs: build-test
    name: nuget org push
    environment:
      name: "NuGetEnv"
      url: "https://www.nuget.org/packages/Arc.HttpHealthCheckDashboard.DI"
    steps:
    - name: Checkout code
      uses: actions/checkout@v2      

    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }} 
        
    - name: Build
      run: dotnet build Arc.HttpHealthCheckDashboard.DI/Arc.HttpHealthCheckDashboard.DI.csproj --configuration ${{ env.CONFIGURATION }}

    - name: Create nuget package
      run: dotnet pack Arc.HttpHealthCheckDashboard.DI/Arc.HttpHealthCheckDashboard.DI.csproj --configuration ${{ env.CONFIGURATION }}

    - name: Push nuget package
      run: dotnet nuget push "Arc.HttpHealthCheckDashboard.DI/bin/Release/Arc.HttpHealthCheckDashboard.DI.1.0.0.nupkg" --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate